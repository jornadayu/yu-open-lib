import pkg from '../../package.json'
import { Meta } from '@storybook/addon-docs/blocks'

<Meta title={`${pkg.version}/AppTheme`} />

### AppTheme Usage

```tsx
import React from 'react'

import { ThemeProvider } from '@material-ui/core/styles'
import { CssBaseline } from '@material-ui/core'

import 'yu-open-lib/dist/index.css'
import { AppTheme } from 'yu-open-lib'

const theme = AppTheme({ darkMode: false })

const App = () => {
  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />

      ...
    </ThemeProvider>
  )
}
```

- [Read More about `Material-UI`'s ThemeProvider](https://material-ui.com/customization/theming/#theme-provider)
