import pkg from '../../package.json'
import readme from '../../README.md'
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'

<Meta title={`${pkg.version}/Intro`} />

# Intro

With `useToggle`


```bash
yarn add yu-open-lib@{pkg.version}
```
